{
    "name": "file-name",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "setup": "npm install",
        "start": "webpack serve --config webpack.dev.js",
        "build": "webpack --config webpack.prod.js",
        "lint-format": "biome check . --fix",
        "deploy": "git checkout gh-pages || git checkout -b gh-pages && git merge main --no-edit && npm run build && git add -f dist && git commit -m 'Initial gh-pages deploy' && git subtree push --prefix dist origin gh-pages && git checkout main && rm -rf dist",
        "reset": "git push origin --delete gh-pages && git branch -D gh-pages"},
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@biomejs/biome": "2.3.4",
        "clean-webpack-plugin": "^4.0.0",
        "css-loader": "^7.1.2",
        "html-loader": "^5.1.0",
        "html-webpack-plugin": "^5.6.4",
        "style-loader": "^4.0.0",
        "webpack": "^5.102.1",
        "webpack-cli": "^6.0.1",
        "webpack-dev-server": "^5.2.2"
    },
    "dependencies": {
        "crypto-browserify": "^3.12.1"
    }
}
