{
	"name": "file-name",
	"version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "setup": "npm install",
        "start": "webpack serve --config webpack.dev.js",
        "build": "webpack --config webpack.prod.js",
        "lint-format": "biome check . --fix",
        "first-deploy": "npm run build && git add -f dist && git commit -m 'Initial gh-pages deploy' && git subtree push --prefix dist origin gh-pages --force && git reset HEAD~1 && rm -rf dist",
        "deploy": "npm run build && git push origin `git subtree split --prefix dist HEAD`:gh-pages --force && rm -rf dist"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "module",
    "devDependencies": {
        "@biomejs/biome": "2.3.4",
        "clean-webpack-plugin": "^4.0.0",
        "css-loader": "^7.1.2",
        "html-loader": "^5.1.0",
        "html-webpack-plugin": "^5.6.4",
        "style-loader": "^4.0.0",
        "webpack": "^5.102.1",
        "webpack-cli": "^6.0.1",
        "webpack-dev-server": "^5.2.2"
    },
    "dependencies": {
        "crypto-browserify": "^3.12.1"
    }
}
